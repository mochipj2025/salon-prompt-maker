# Testing Checklist

公開前、またはテストユーザーに触ってもらう時の確認リストです。

## 基本操作

- `index.html` が開ける
- `guide.html` が開ける
- トップから使い方ガイドへ移動できる
- ガイドからPromptメーカーへ戻れる
- Promptをコピーできる
- 入力内容が再読み込み後も残る

## 用途別

- note見出し画像で `16:9` が出る
- Instagram投稿で `4:5` が出る
- リール表紙とストーリーズで `9:16` が出る
- LPファーストビューで `16:9` が出る
- サロンPOP、図解、比較画像で `4:5` が出る

## ユーザー想定別

- リラクゼーションサロン
- 整体・ボディケア
- 美容サロン
- カフェ・バー
- note記事
- SNS発信

## ブランド設定

- 店名を入れるとPromptに反映される
- ブランド色・雰囲気を入れるとPromptに反映される
- 固定キャラのチェックが未選択ならPromptにキャラ指示が出ない
- 固定キャラのチェックを入れると説明欄が表示される
- 実店舗写真のチェックを入れると、店舗写真添付の案内が表示される

## 安全表現

- `治療` を入力すると警告される
- `改善` を入力すると警告される
- `矯正` を入力すると警告される
- `必ず` を入力すると警告される
- 言い換え案が表示される

## 表示確認

- iPhone 12 Pro相当の幅で文字がはみ出さない
- 出力欄が読める
- 右下のもちスラが操作を邪魔しない
- ガイドページがモバイルで読みやすい
